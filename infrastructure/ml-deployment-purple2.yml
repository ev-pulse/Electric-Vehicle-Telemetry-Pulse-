# ============================================================
# EV-Pulse — ML Online Deployment 재현 파일
# ============================================================
# 엔드포인트  : ev-anomaly-endpoint-6403dedf
# 배포명      : purple2  (프로덕션 최종 버전)
# 모델        : ev-lgbm-inference-artifact  v8
# 알고리즘    : LightGBM (BSI 기반 배터리 이상 탐지)
# 워크스페이스: ev-modeling-ML  (koreacentral)
#
# [재배포 방법]
#   1. 엔드포인트 먼저 생성 (없을 경우)
#      az ml online-endpoint create \
#        --name ev-anomaly-endpoint-6403dedf \
#        --workspace-name ev-modeling-ML \
#        --resource-group 4dt_team_1
#
#   2. 이 파일로 배포 생성
#      az ml online-deployment create \
#        --file infrastructure/ml-deployment-purple2.yml \
#        --workspace-name ev-modeling-ML \
#        --resource-group 4dt_team_1 \
#        --all-traffic
# ============================================================

$schema: https://azuremlschemas.azureedge.net/latest/managedOnlineDeployment.schema.json

name: purple2
endpoint_name: ev-anomaly-endpoint-6403dedf

# 프로덕션 모델 — LightGBM BSI 이상 탐지 최종 버전
model: azureml:ev-lgbm-inference-artifact:8

instance_type: Standard_DS2_v2
instance_count: 1

request_settings:
  request_timeout_ms: 90000
  max_concurrent_requests_per_instance: 1

liveness_probe:
  initial_delay: 10
  period: 10
  timeout: 2
  success_threshold: 1
  failure_threshold: 30

readiness_probe:
  initial_delay: 10
  period: 10
  timeout: 2
  success_threshold: 1
  failure_threshold: 30
